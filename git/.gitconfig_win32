[core]
  whitespace = cr-at-eol
[merge]
    tool = kdiff3
[mergetool "kdiff3"]
    cmd = sh ~/.dotfiles/windows-shell/Scripts/kdiff3.sh "$BASE" "$LOCAL" "$REMOTE" -o "$MERGED"
    trustExitCode = false
